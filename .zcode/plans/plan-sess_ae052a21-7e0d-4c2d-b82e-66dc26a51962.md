## Goal

Upgrade Marsky from strictly-2-person pairs to real groups (3–8 members per list), per your decisions:

1. **Done rule:** unchanged — a task completes only when *all* members confirm "done" (the code is already N-member generic; only copy says "both").
2. **Max members:** 8 per list.
3. **List switcher:** sidebar section listing every list you belong to (pair or group); click to switch. The data layer already stores multiple pairs (`state.pairs` + `activeCode`) — this just surfaces it, plus a create/join entry point.

Key exploration finding: the core logic (check-in quorum, `Pair.members[]`, DB reads, `pickColor` rotation, item cards, history) is already member-count-agnostic. The 2-person limit lives in exactly: two SQL spots, one line in `pair-store.ts`, singular "partner" UI patterns in `app/app/page.tsx`, and "two people/both/partner" copy throughout.

## Changes

### 1. DB migration — new `web/supabase/migrations/0003_groups.sql`
- Add `pairs.kind text not null default 'pair' check (kind in ('pair','group'))`.
- Recreate `join_pair` RPC: `cnt >= 8 → 'full'` (was `>= 2`).
- Recreate RLS policy "pm insert self": `count(*) < 8` (was `< 2`), keeping the qualified-correlation fix from 0002.
- Idempotent (safe to re-run), same style as 0002.

### 2. `web/lib/db.ts`
- `dbCreatePair` writes `kind`; `dbFetchPair` reads it into the `Pair` object.

### 3. `web/lib/types.ts`
- `Pair` gains `kind: "pair" | "group"`.
- Update the two 2-person comments (`completedLog`, `checkin`).

### 4. `web/lib/pair-store.ts`
- `joinPair`: cap `>= 8` (was `>= 2`); comment no longer "member 2".
- `createPair` / `createPairAsync`: accept `kind` (default `"pair"`); default name `"${user.name} & partner"` for pairs, `"${user.name}'s group"` for groups.
- `demoPartner`: cap `>= 8` so dev simulation can fill a group.
- Copy in history strings: `"with both members"` → `"— everyone said done"`; `"to both of you"` → `"to everyone"`.
- **New `setActiveCode(code)`**: validates membership, switches `activeCode`, calls `watchPair(code)` when the DB is available (realtime + poll follow the new list). Existing `getPairs()` becomes the switcher's data source.

### 5. `web/app/app/page.tsx` (main list)
- Replace singular `partner` with `others` (all members except you):
  - Sidebar "Your pair" block → avatar stack of **all** members (overlapping, `+N` beyond 4), caption "N members · reminders reach everyone".
  - Header avatar cluster: render all members instead of you + one partner + "?".
  - Heading: `pair.name || "Our list"` whenever `members.length > 1`; invite banner stays for solo lists only.
- **New sidebar "Your lists" section** (above "Your pair"): one row per pair in `getPairs()` — name (or "Untitled list"), kind glyph, member count; active row highlighted; click → `setActiveCode`; a "+ New" row routes to `/` (landing create/join).
- Join celebration: fire on any membership increase (toast "{name} joined the list"), not just the 1→2 transition.
- Copy sweep: "both of you" → "everyone" (added/deleted toasts, empty state), "joined the pair" → "joined the list", notification title "from your partner" → "from your list", delete-modal "for both of you" → "for everyone", tagline → "One list, together, zero nagging."

### 6. Landing + join (`web/app/page.tsx`, `web/components/join-form.tsx`)
- Create form: a two-option chooser — **Pair** ("just you two") and **Group** ("up to 8 people") — radio-style cards matching existing pill/button styling; passes `kind` to `createPairAsync`.
- "Full" error copy: "This list is full (8 members max)."
- Hero copy adjusted: "a shared reminder list for two" → "for two — or the whole crew"; keep brand voice otherwise.

### 7. Small copy fixes in components
- `compose-bar.tsx`: "Remind both at" → "Remind everyone at".
- `checkin-modal.tsx`: "Reminded you both" → "Reminded everyone"; "You both did it —" → "You all did it —"; "One of you isn't done yet." → "Not everyone is done yet."; "Remind us both again in…" → "Remind everyone again in…".

### 8. Docs (minimal truthfulness patch)
- `prd.md` §6.3: move "more than 2 members" out of out-of-scope with a short note (groups supported, 8 max, everyone-confirms rule).
- `ARCHITECTURE.md` §13 risk lines about "2-member cap" / "groups (PRD says no)": update to reflect the 8-cap.

`item-card.tsx`, `history-modal.tsx`, `avatar.tsx`, sounds, and the whole DB sync engine need **no** changes.

## Verification

- `npx eslint` on all changed files + `npm run build`.
- Live preview in the side panel: create a group list, use the dev "Simulate partner" repeatedly to reach 3–4 members, verify: avatar stacks render all members, check-in modal shows one row per member and completes only when all say done, switcher switches lists (realtime follows), join celebration fires per join, no horizontal overflow, dark mode still consistent.
- Migration file is delivered for you to apply to Supabase (`supabase db push` or SQL editor) — I can't run your remote DB. Until applied, the DB still caps joins at 2 via the RPC; local/demo mode uses the new 8-cap immediately.